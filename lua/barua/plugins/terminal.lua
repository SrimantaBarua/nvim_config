return {
    -- Toggle multiple terminals during an editing session
    {
        "akinsho/toggleterm.nvim",
        version = "*",
        opts = {
            open_mapping = "<C-\\>t"
        }
    }
}
